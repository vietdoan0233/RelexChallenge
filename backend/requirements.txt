# Pinned against Python 3.14.7 on 2026-09-18. All wheels installed cleanly
# with no build-from-source fallback, so no version was chosen defensively.
fastapi==0.141.1
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
numpy==2.5.3
python-dotenv==1.2.3
httpx==0.28.1
pytest==9.1.1
ruff==0.16.8

# Architecture v1.6 (CLAUDE.md 18.0.3): authenticated encryption for the
# isolated reversal vault (app/privacy/vault.py). Vetted library, no
# homemade cryptography. Pinned against 50.0.1 on 2026-09-20.
cryptography==50.0.1
