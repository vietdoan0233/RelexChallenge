# Safe placeholders only. Copy this file to .env and put secrets there.
# The organizer-provided GPT endpoint/model contract is still pending.
GPT_API_KEY=
GPT_BASE_URL=
GPT_MODEL=
GPT_EMBEDDING_MODEL=
DATABASE_PATH=./data/keeper.db
SOURCE_DATA_DIR=./data/source
