# Vercel's root Python runtime installs this file for api/index.py.
# Keep these runtime pins aligned with backend/requirements.txt. Test and
# lint-only packages stay backend-only so the deployed function stays small.
fastapi==0.141.1
pydantic==2.13.5
pydantic-settings==2.15.0
numpy==2.5.3
python-dotenv==1.2.3
httpx==0.28.1
python-multipart==0.0.32
cryptography==50.0.1
