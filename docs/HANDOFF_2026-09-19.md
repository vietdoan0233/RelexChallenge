# KEEPER session handoff — 2026-09-19

> **Superseded snapshot.** This file records the repository state *before*
> Phase 1 identity hardening. The counts below (74 people, 234 aliases, 42
> tests) are historical and no longer current. `CLAUDE.md`/`AGENTS.md`
> section 0.1 is the current, authoritative checkpoint.

## Where work stopped

KEEPER is stopped at the mandatory **Phase 1 Evidence Locker review/hardening gate**. Phase 2 retrieval has not started, and no Project Pulse, reasoning, Skeptic, deletion, or final UI work has been started early.

The frozen architecture is now **v1.4**. The only architectural change from v1.3 is the user-directed provider switch from Google/Gemini to the organizer-provided GPT service. Its API key, base URL, reasoning model, and embedding model remain placeholders until the organizers provide the exact contract.

## Repository state at handoff

- Branch: `main`
- Remote: `origin` → `https://github.com/vietdoan0233/RelexChallenge.git`
- Earlier milestone commits:
  - `5886ab5 chore(scaffold): establish keeper phase 0 baseline`
  - `1c6f3d7 feat(ingestion): build evidence locker and stable source parsing`
  - `1bb96b1 fix(ingestion): strip bullet numbering in single-bullet report sections`
  - `c56c252 docs: record phase 1 checkpoint and handoff`
- Phase 0 is complete and verified.
- Phase 1 is implemented and verified offline, but not yet accepted through the review gate.
- `data/keeper.db`, `.env`, virtual environments, caches, and frontend build output remain gitignored.

## Implemented foundation

- SQLite schema and repository layer
- Deterministic ingestion from the canonical `data/source/` boundary
- Stable source-locator manifest and Evidence IDs
- Named Teams transcript parsing with deterministic UI-chrome cleanup
- Anonymous INTERNAL `Me:` / `Them:` parsing without identity inference
- Email-thread parsing, including English, German, and Swedish Outlook headers
- Fine-grained report bullet/sub-paragraph units
- Truncation preservation through `is_truncated`
- People, aliases, and `AUTHOR` / `SPEAKER` / `MENTIONED` relationships
- FTS5 population
- Provider-neutral embedding interface
- Deterministic mock embeddings and `--skip-embeddings`
- Ingestion CLI and count/warning report
- Unit and integration tests

## Latest verified offline results

- Documents: 45 total
  - 23 transcripts
  - 20 email threads
  - 2 report threads
- Evidence Units: 2,517 total
  - 2,115 transcript units
  - 110 email-message units
  - 292 report units
- FTS rows: 2,517
- Real embedding rows: 0
- Latest identity output: 74 people and 234 aliases
- Quality gate rerun successfully at session close:
  - 42 backend tests passed
  - Ruff check passed
  - Ruff format check passed
  - frontend typecheck/build passed

## Why Phase 1 is not accepted yet

### 1. Identity discovery is too permissive

The capitalized-phrase scan promoted obvious non-person phrases such as `Risk Fresh Phase`, `Slight Delay Bakery`, `Not Nadia Öberg`, and `This So` into people. That can create false `MENTIONED` relationships and excessive deletion collateral.

The next implementation pass must:

1. seed identities from deterministic roster, attendee, speaker, sender, and email sources,
2. treat free-text matches as candidates rather than automatically deletion-relevant people,
3. store only reviewed/high-confidence mappings,
4. leave ambiguous aliases unresolved,
5. rerun and manually inspect the people/alias/evidence-person report.

### 2. Real embeddings are missing

The mock embedding path is tested. The organizer GPT transport is deliberately not guessed before its API contract is available. Before semantic retrieval begins:

1. obtain the organizer API key, base URL, and supported GPT model names,
2. implement the thinnest direct adapter that matches the supplied contract,
3. create a local root `.env` from the tracked, secret-free `.env.example`,
4. set `GPT_API_KEY`, `GPT_BASE_URL`, `GPT_MODEL`, and `GPT_EMBEDDING_MODEL` in `.env`,
5. keep `.env` uncommitted,
6. run one real embedding smoke test,
7. generate and verify embedding rows for the intended corpus,
8. report any partial failures rather than hiding them.

## Direction for the next session

Continue **Phase 1 only** until its foundation is trustworthy:

1. inspect local `main`; if remote changes must be incorporated, tell the user to run `git pull --ff-only`,
2. fix high-confidence people/alias discovery,
3. add regression tests for the false-positive identities found in the real corpus,
4. configure and verify real embeddings when credentials are available,
5. rerun ingestion from `data/source/`,
6. verify document, Evidence Unit, FTS, embedding, people, alias, and relationship counts,
7. run backend tests, Ruff, frontend typecheck, and frontend build,
8. issue a corrected Phase 1 review report,
9. wait for review before starting Phase 2.

Once Phase 1 is accepted, Phase 2 remains exactly as frozen in `CLAUDE.md`: SQLite FTS5 BM25, NumPy cosine similarity, deterministic rank fusion, neighbor expansion, temporal sweep, and a benchmark across at least 10 known topics/questions.

## Restart checklist

```powershell
git status --short --branch
git log --oneline --decorate --graph --all -10
```

Confirm that no unexpected working-tree changes exist before continuing. The user performs pull/fetch/push operations when asked. Never use the untouched original archive as a runtime rebuild fallback; normal ingestion and deletion work only from `data/source/`.
